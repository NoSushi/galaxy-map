I need you to audit and modify this project so it is genuinely deployable on my production server, not just on Replit.

### Production environment

The website will ultimately run in Docker on this server:

* Linux x86_64
* Debian/Ubuntu-based Docker container
* Node.js 20
* Docker
* 1 vCPU
* 1 GB RAM
* KVM virtual machine
* The VM exposes an **old x86-64 CPU profile with only baseline x86-64 instruction support**
* It does NOT provide modern CPU features such as SSE4.1, SSE4.2, POPCNT, AVX, AVX2, etc.
* I cannot change the VM CPU configuration.
* The application must therefore work on **x86-64-v1 / baseline x86-64**, without assuming a modern processor.
* The production Docker image must build and run independently of Replit.

### Important requirement

Do a full production portability audit of the application.

Do NOT assume that because something works in Replit that it will work on the production VM.

In particular, identify every dependency that:

* Contains native binaries
* Downloads platform-specific binaries during npm install
* Uses CPU-specific instruction sets
* Has optional native dependencies
* Uses prebuilt binaries that may require SSE4.2, AVX, AVX2, or x86-64-v2/v3
* Depends on Replit-specific package registries, URLs, environment variables, paths, or build tooling
* Requires services or packages that will not exist in the production Docker container

### Sharp is especially important

The application currently uses Sharp for image validation and conversion to WebP.

Sharp MUST remain available because it is required by the application.

Do not simply remove Sharp or remove the image validation/conversion functionality.

However, the current Sharp version may select a Linux x64 prebuilt binary that requires CPU features unavailable on the production VM.

Find the most reliable solution that allows the application to use Sharp on this baseline x86-64 production environment.

Possible solutions to investigate include:

1. A supported Sharp/WebAssembly backend.
2. Building Sharp/native dependencies from source with a baseline x86-64 target.
3. A Sharp version whose supported binaries are compatible with baseline x86-64.
4. Another production-safe implementation if the above are genuinely unsuitable.

Prefer the solution that is:

* Supported
* Reproducible
* Stable
* Easy to build in Docker
* Not dependent on Replit
* Not dependent on changing the production VM CPU
* Reasonable for a 1 GB RAM server

Do NOT just suppress Sharp errors or make the application start while image validation silently fails.

### npm/package-lock requirements

Audit package-lock.json for any Replit-specific registry URLs such as:

package-firewall.replit.internal

The production lockfile MUST use publicly accessible/reproducible package sources.

Do not leave dependencies pointing at Replit-internal infrastructure.

Make sure `npm ci` works from a clean checkout using the public npm registry.

### Docker requirements

Make the Docker build reproducible.

The Dockerfile should:

* Use Node 20
* Install dependencies reproducibly
* Build the frontend/server
* Produce a production-ready image
* Not require Replit
* Not require developer-specific files
* Not depend on packages already installed on the host
* Not depend on the CPU being newer than baseline x86-64

If native compilation is required, install all required build dependencies explicitly in the Dockerfile and use a multi-stage build if appropriate.

If a native package can accidentally download an incompatible prebuilt binary, configure the build so that cannot happen.

### Resource constraints

The production server only has:

* 1 CPU core
* 1 GB RAM
* 512 MB swap

Avoid unnecessarily memory-heavy build/runtime dependencies.

The application itself must remain fully functional; do not remove features merely to make it fit.

### Testing requirement

Before considering this complete, test the project as though it were being deployed to the production server.

At minimum:

1. Start from a clean dependency installation.
2. Run `npm ci`.
3. Run the production build.
4. Build the Docker image.
5. Start the container.
6. Verify the server actually starts.
7. Verify Sharp can be imported successfully.
8. Verify the Sharp functionality actually used by the application works:

   * image validation
   * image metadata/dimension inspection
   * WebP conversion
9. Verify the application can serve HTTP requests.
10. Check that there are no Replit-specific runtime dependencies.

For native dependencies, explicitly check that the resulting binaries do not require CPU instructions unavailable on baseline x86-64.

### Do not just tell me what is wrong

Actually make the required changes to the repository.

After making the changes, give me:

1. A summary of what you changed.
2. Every dependency you changed and why.
3. The final Sharp strategy and why it is compatible with the production CPU.
4. Any Dockerfile changes.
5. Any package.json/package-lock.json changes.
6. Exact commands I should run on the production server to deploy it.
7. Any remaining limitations or risks.

The goal is:

**A clean Git checkout → npm ci → Docker build → Docker run → working production website on the old baseline x86-64 VM, without Replit being required at runtime or during deployment.**
